`verilator_config

lint_off -rule PINCONNECTEMPTY -file "*/el2_tmr_recovery_fsm.sv" -lines 210
lint_off -rule PINCONNECTEMPTY -file "*/el2_tmr_recovery_fsm.sv" -lines 245
lint_off -rule UNUSEDPARAM
lint_off -rule LATCH
lint_off -rule UNOPTFLAT
