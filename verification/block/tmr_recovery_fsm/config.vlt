`verilator_config

lint_off -rule PINCONNECTEMPTY -file "*/el2_tmr_recovery_fsm.sv" -lines 217
lint_off -rule PINCONNECTEMPTY -file "*/el2_tmr_recovery_fsm.sv" -lines 254
lint_off -rule PINCONNECTEMPTY -file "*/el2_tmr_recovery_fsm.sv" -lines 293
lint_off -rule UNUSEDPARAM
lint_off -rule LATCH
lint_off -rule UNOPTFLAT
